import React from "react";
import Add from "../media/icons/Add.js";

export default function SpecialPage(props) {
  return (
    <div className="flex-center-btw">
      <button className="icon" style={{ marginLeft: "8px" }}>
        <Add />
      </button>
    </div>
  );
}
